//Write a C++ program that needs to calculate the total count of the occurrences of
//letters in a string. The occurrences must be in a form of a pair of letters, preferred
//method of solving this question is to use a character array.
#include <iostream>
using namespace std;
int main(){
    string s;
    cout<<"enter the text: ";
    cin>>s;
    int j=sizeof s;
    char c[2];
    for(int i=0;i<j;i++){
        if(s[i]==s[i+1]){
            c[i]=s
        }
    }
}
